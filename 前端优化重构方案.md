# 🎨 Dev-Linker UI 重构计划书 (V2.0 Design)





## Phase 0: 技术底座升级 (Tailwind CSS)



目前项目是纯 CSS + Element Plus，为了实现细腻的现代 UI，我们需要引入原子化 CSS 引擎。



### 1. 安装依赖



在项目根目录运行：

Bash

```
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```



### 2. 配置 Tailwind



修改生成的 `tailwind.config.js`，定义我们的**设计系统**（配色与圆角）：

JavaScript

```
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./index.html",
    "./src/**/*.{vue,js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        // 定义品牌色：清爽的科技蓝
        brand: {
          50: '#eff6ff',
          100: '#dbeafe',
          500: '#3b82f6',
          600: '#2563eb', // 主色
          700: '#1d4ed8',
        },
        // 定义中性色：用于边框和背景，避免死灰
        slate: {
          50: '#f8fafc', // 侧边栏背景
          100: '#f1f5f9', // 锁定状态背景
          200: '#e2e8f0', // 边框
          800: '#1e293b', // 主文字
          400: '#94a3b8', // 提示文字
        }
      },
      boxShadow: {
        'sm-soft': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'card': '0 4px 6px -1px rgba(0, 0, 0, 0.02), 0 2px 4px -1px rgba(0, 0, 0, 0.02)',
      }
    },
  },
  plugins: [],
}
```



### 3. 引入样式



在 `src/renderer/src/assets/main.css` (或新建) 中添加：

CSS

```
@tailwind base;
@tailwind components;
@tailwind utilities;

/* 解决 Element Plus 样式冲突 */
body {
  @apply text-slate-800 bg-white;
}
```

并在 `main.ts` 中引入该 css 文件。

------



## Phase 1: 全局布局重构 (App Shell)



我们将废弃旧的左右结构，采用 **“顶部指令台 + 侧边栏 + 主内容区”** 的经典布局。



### 设计图解



```
[ 顶部指令台 (Command Bar) - h-16 border-b ]
---------------------------------------------------
[ 左侧 ]          |  [ 右侧主内容区 ]
[ 配置 ]          |  
[ w-80 ]          |  [ 日志流 (Log Stream) ]
[ bg-slate-50 ]   |  [ flex-1 ]
[ border-r ]      |  
                  |--------------------------------
                  |  [ 发送区 (Send Box) - h-auto ]
```



### 关键代码参考 (`MainView.vue`)



HTML

```
<template>
  <div class="flex flex-col h-screen bg-white overflow-hidden">
    
    <header class="h-16 border-b border-slate-200 flex items-center px-4 justify-between bg-white shadow-sm z-10">
      <div class="flex items-center gap-4">
        <div class="font-bold text-xl tracking-tight text-brand-600 flex items-center gap-2">
          <el-icon><Connection /></el-icon> Dev-Linker
        </div>
        <div class="h-6 w-px bg-slate-200 mx-2"></div>
        
        <div class="flex gap-2">
          <el-select v-model="protocol" class="w-32" size="default">...</el-select>
          <el-input v-model="address" class="w-64 font-mono" placeholder="127.0.0.1:8080">
            <template #prefix><span class="text-slate-400 text-xs">ws://</span></template>
          </el-input>
          <button 
            class="px-6 py-1.5 rounded-md text-sm font-medium transition-all shadow-sm-soft"
            :class="isConnected ? 'bg-red-50 text-red-600 border border-red-200 hover:bg-red-100' : 'bg-brand-600 text-white hover:bg-brand-700 shadow-blue-200'"
          >
            {{ isConnected ? '断开' : '连接' }}
          </button>
        </div>
      </div>

      <div class="flex items-center gap-6">
         <div class="flex gap-4 text-xs font-mono text-slate-500">
            <div class="flex flex-col items-end">
              <span class="text-[10px] text-slate-400 uppercase">TX</span>
              <span class="font-bold text-slate-700">1.2 KB</span>
            </div>
            <div class="flex flex-col items-end">
               <span class="text-[10px] text-slate-400 uppercase">RX</span>
               <span class="font-bold text-brand-600">842 B</span>
            </div>
         </div>
         <div class="flex gap-0.5 items-end h-4" title="虚拟信号强度: -65dBm">
           <div class="w-1 h-1 bg-green-500 rounded-sm"></div>
           <div class="w-1 h-2 bg-green-500 rounded-sm"></div>
           <div class="w-1 h-3 bg-green-500 rounded-sm"></div>
           <div class="w-1 h-4 bg-slate-200 rounded-sm"></div> </div>
      </div>
    </header>

    <div class="flex flex-1 overflow-hidden">
      
      <aside class="w-80 bg-slate-50 border-r border-slate-200 flex flex-col overflow-y-auto p-4 gap-4">
         <ConnectionConfig />
      </aside>

      <main class="flex-1 flex flex-col min-w-0 bg-white">
         <DataInteraction />
      </main>

    </div>
  </div>
</template>
```

------



## Phase 2: 左侧配置面板优化 (解决“看不清”)



**核心策略：** 放弃 Element Plus 默认的 disabled 样式。手动控制锁定状态的 UI。



### 1. 视觉处理



- **锁定状态**：不降低透明度。背景变为浅灰 (`bg-slate-100`)，文字颜色保持深灰 (`text-slate-700`)，在右上角显示一个小锁图标。
- **卡片式设计**：每个配置块（SN、心跳、登录）作为一个独立的白色卡片 (`bg-white rounded-lg border border-slate-200 p-4`)。



### 2. 代码参考 (`ConnectionConfig.vue`)



HTML

```
<template>
  <div class="bg-white rounded-xl border border-slate-200 shadow-sm-soft overflow-hidden transition-all duration-200 hover:shadow-card">
    <div class="flex justify-between items-center px-4 py-3 border-b border-slate-100 bg-slate-50/50">
      <div class="flex items-center gap-2">
        <div class="w-1.5 h-1.5 rounded-full bg-brand-500"></div>
        <span class="text-sm font-semibold text-slate-700">心跳包</span>
      </div>
      <el-switch v-model="enabled" size="small" :disabled="isLocked" />
    </div>

    <div class="p-4 relative">
      
      <div v-if="isLocked" class="absolute inset-0 bg-slate-50/80 backdrop-blur-[1px] z-10 flex items-center justify-center cursor-not-allowed">
         </div>

      <div :class="isLocked ? 'pointer-events-none select-none' : ''">
        
        <div v-if="isLocked" class="flex flex-col gap-2">
           <div class="flex justify-between text-sm border-b border-slate-100 pb-2">
              <span class="text-slate-500">间隔</span>
              <span class="font-mono font-medium text-slate-800">{{ interval }}s</span>
           </div>
           <div class="flex flex-col gap-1 mt-1">
              <span class="text-xs text-slate-400">发送内容 ({{ format }})</span>
              <div class="bg-slate-100 text-slate-700 font-mono text-sm p-2 rounded border border-slate-200">
                 {{ content }}
                 <el-icon class="absolute right-2 top-2 text-slate-400"><Lock /></el-icon>
              </div>
           </div>
        </div>

        <div v-else class="flex flex-col gap-3">
           </div>

      </div>
    </div>
  </div>
</template>
```

------



## Phase 3: 右侧日志与发送区



为了符合“极简”风格，日志区要像一张干净的纸。

1. **日志区 (Log)**:
   - 背景：纯白。
   - 字体：`font-mono` (JetBrains Mono 或 Consolas)。
   - 条目样式：不要斑马纹，使用极细的分隔线。
   - **颜色区分**：
     - 发送 (TX): `text-blue-600` (左对齐)
     - 接收 (RX): `text-emerald-600` (左对齐，或者尝试对话气泡式布局)
     - 系统: `text-slate-400 italic`
2. **发送区 (Send)**:
   - 固定在底部，带有轻微的顶部阴影。
   - 输入框去除默认边框，改为聚焦时底部出现蓝条。

